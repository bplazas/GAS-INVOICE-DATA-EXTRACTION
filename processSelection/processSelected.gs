// test function used during development - do not use

function processSelectedInvoices(selectedIndices, jsonFileName) {
  console.log("processSelectedInvoices called with indices:", selectedIndices);
  return `Selected ${selectedIndices.length} invoices for processing. (Stub)`;
}
